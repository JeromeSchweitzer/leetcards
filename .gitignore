# Python
.venv/
__pycache__/
*.pyc

# Data pipeline + scraped dataset are local-only (not published). The app ships
# only the committed App/Resources/dataset.sample.json.
pipeline/
App/Resources/dataset.json

# macOS
.DS_Store

# Xcode — project is generated from project.yml via XcodeGen, so the
# .xcodeproj itself is a build artifact and is not tracked.
*.xcodeproj/
.build/
.swiftpm/
DerivedData/
xcuserdata/
